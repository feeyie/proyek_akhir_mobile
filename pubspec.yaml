name: tripify
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  font_awesome_flutter: ^10.7.0
  cached_network_image: ^3.3.1
  http: ^1.5.0
  url_launcher: ^6.2.1
  flutter_map: ^7.0.2
  latlong2: ^0.9.0
  geolocator: ^13.0.1
  geocoding: ^3.0.0
  provider: ^6.1.2

  # Untuk autentikasi & penyimpanan
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  shared_preferences: ^2.2.3
  crypto: ^3.0.3
  path_provider: ^2.1.3
  supabase_flutter: ^2.10.3
  image_picker: ^1.0.0
  flutter_local_notifications: ^19.5.0
  google_fonts: ^6.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  hive_generator: ^2.0.1  
  build_runner: ^2.4.9

flutter:
  uses-material-design: true
  assets:
    - assets/